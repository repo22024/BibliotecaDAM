name: Primera automatizacion

on:
  push:
    branches:
      - main

jobs:
  saludar:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar mensaje
        run: echo "Hola, soy una automatizacion 🚀"
